repos:
  - repo: local
    hooks:
      - id: forbid-org-files
        name: forbid org-mode files
        entry: bash -c 'echo "Do not commit .org files." >&2; exit 1'
        language: system
        files: \.org$
